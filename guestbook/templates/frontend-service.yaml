apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}-frontend   # ← was {{ .Release.Name }}-frontend
spec:
  selector:
    app: {{ .Values.appName }}-frontend
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
  type: ClusterIP