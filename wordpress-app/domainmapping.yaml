apiVersion: serving.knative.dev/v1beta1
kind: DomainMapping
metadata:
  name: wordpress.knative.local
  namespace: default
spec:
  ref:
    name: wordpress-wordpress
    kind: Service
    apiVersion: serving.knative.dev/v1